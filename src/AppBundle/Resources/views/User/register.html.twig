{% extends 'base.html.twig' %}

{% block body %}

<h1 class="header center teal-text text-lighten-1">Ajouter un compte</h1>

	<div class="container">
	{{ form_start(form, { 'attr': {'class': 'container'}}) }}
	{{ form_widget(form)}}
	<br><br><br>
	{{ form_end(form) }}
	</div>
{% endblock %}
